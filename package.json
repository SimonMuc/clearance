{
  "name": "clearance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A little space for what matters.",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "check": "node --check src/store.js && node --check src/server.js && node --check public/app.js && node --check public/sw.js",
    "test": "node --test test/*.test.js",
    "test:browser": "node scripts/browser-test.js",
    "icons": "node scripts/make-icons.js"
  }
}
